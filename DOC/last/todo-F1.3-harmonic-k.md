## ТЗ — F1.3 follow-up: `fix: harmonic k` (исчерпан)

**Статус:** закрыт измерением, не зелёным тестом. Следующий патч — [`todo-F1.3-chain-dod.md`](todo-F1.3-chain-dod.md). Канон — [ADR-020 §3 errata 2](../ADR/ADR-020-Subtract-Phi-Gradient-Pass.md).

Узкий патч был исполнен: только `SubtractPhiGradientPassTests.cs`, `HarmonicK` 8 → 12, имя `ProjectionChain_HarmonicK12_...`. Пасс / `.compute` / 3.1–3.5 / Jacobi×40 / assert `/10` не трогались.

### Замер k=12 (N=64, Size=32, Jacobi×40)

| | k=8 (до) | k=12 (этот патч) |
| --- | --- | --- |
| гейт `\|mean\|/max` | 0.018 | 0.019 |
| maxBefore | 2.613 | 3.625 |
| maxAfter | 0.586 | 1.453 |
| отношение | **4.46×** | **2.49×** |

Выше k — хуже `max|D|`. Теория `λ^40 ≈ 33×` описывает Φ, не D. k второй раз не крутить.

### Что было в исходном ТЗ (не повторять)

Поднять k до 12, оставить порог 10×. Если красный — стоп, архитектура. Это случилось. Не исполнять этот файл заново.
